<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.foursure.one.hogwarts">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name="net.foursure.hogwarts.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="net.foursure.hogwarts.MainActivity"
            android:label="@string/hogwarts_home"
            android:theme="@style/AppTheme" />
        <activity
            android:name="net.foursure.hogwarts.HouseActivity"
            android:label="@string/house"
            android:theme="@style/AppTheme" />
        <activity
            android:name="net.foursure.hogwarts.StudentRegistryActivity"
            android:label="@string/stud_registry"
            android:theme="@style/AppTheme" />
        <activity
            android:name="net.foursure.hogwarts.SpellsActivity"
            android:label="@string/spell"
            android:theme="@style/AppTheme" />
        <activity
            android:name="net.foursure.hogwarts.HouseDetailsActivity"
            android:label="@string/house"
            android:theme="@style/AppTheme"/>
    </application>

</manifest>